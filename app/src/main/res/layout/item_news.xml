<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="15dp"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/img"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:src="@mipmap/resource"
        android:scaleType="centerCrop"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@color/white"
        android:padding="10dp">

        <TextView
            android:id="@+id/name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="爱心"
            android:textSize="16sp"
            android:textColor="@color/black" />

        <TextView
            android:id="@+id/content"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:text="心爱"
            android:singleLine="false"
            android:maxLines="3" />
        <!--maxLines 是在限制高度， singleLine 是强制不让换行。具体效果有什么区别呢？
            从高度来讲是一样的，两者肯定都显示一行，但从换行的位置来讲就有区别了，
            maxLines并不会改变其换行的位置，而singleLine则会。从这个角度讲，
            singleLine的显示会好一些，因为如果超过一行singleLine会在一行内显示，
            后面加上"...",而maxlines="1" 则不会，它依然会在原来换行的位置换行，
            所以有时候一行不满，但是却不显示剩下的部分。-->
        <!--第一个是显示多少行：android:lines="1"
            第二个是省略的位置：android:ellipsize="end/start"-->

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp">

            <TextView
                android:id="@+id/comment"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:drawableLeft="@mipmap/comment"
                android:drawablePadding="10dp"
                android:maxLines="3"
                android:singleLine="false"
                android:text="123条评论" />

            <TextView
                android:id="@+id/data"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="2020-1-1"
                android:layout_alignParentRight="true"
                android:singleLine="false"
                android:maxLines="3" />

        </RelativeLayout>

    </LinearLayout>


</LinearLayout>































